---
import { Icon } from 'astro-icon/components';
---

<section
  id="hero"
  class="mx-auto max-w-6xl px-4 min-h-[100svh] grid gap-8 md:grid-cols-2 items-center"
>
  <div class="">
    <h1 class="text-center md:text-left font-sans font-medium text-4xl md:text-5xl text-gray-900">
      Sacs <span class="text-accent">personnalisés</span>, <span class="text-accent">print</span> et <span class="text-accent">sites web </span>sur mesure !
    </h1>
    <h2 class="font-serif mt-4 text-lg text-text text-center md:text-left">
      Vos outils de communication visuelle pour la mise en valeur de votre identité
    </h2>

    <!-- Boutons -->
    <div class="mx-12 md:mx-0 mt-6 flex flex-col sm:flex-row gap-4 justify-center md:justify-start" role="group" aria-label="Accès rapide">
      <!-- 1) Print -->
      <a
        href="#print"
        class="inline-flex justify-center items-center gap-2 rounded-full bg-gray-500 text-bg px-4 py-2 hover:opacity-90 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-accent"
      >
        <Icon name="lucide:package" class="w-4 h-4" aria-hidden="true" />
        Print
      </a>

      <!-- 2) Web -->
      <a
        href="#web"
        class="inline-flex justify-center items-center gap-2 rounded-full bg-gray-500 text-bg px-4 py-2 hover:opacity-90 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-accent"
      >
        <Icon name="lucide:code-2" class="w-4 h-4" aria-hidden="true" />
        Web
      </a>

      <!-- 3) Contact -->
      <a
        href="#contact"
        class="inline-flex justify-center items-center gap-2 rounded-full bg-accent text-white px-4 py-2 hover:opacity-90 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-accent"
      >
        <Icon name="lucide:mail" class="w-4 h-4" aria-hidden="true" />
        Contact
      </a>
    </div>
  </div>

    <div class="relative aspect-[4/3]">
        <img
            src="/logo/Logo-bleu.png"
            alt="Exemples d’emballages personnalisés et interface web"
            class="absolute inset-0 h-full w-full object-cover"
            loading="eager"
            decoding="async"
            fetchpriority="high"
        />
    </div>
</section>

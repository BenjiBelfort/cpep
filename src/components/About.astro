---
// src/components/sections/AboutCPEP.astro
import { Icon } from 'astro-icon/components';

type ValueItem = {
  icon: string;
  title: string;
  desc: string;
  badge?: boolean;  // pour mettre un macaron "priorité"
};

const values: ValueItem[] = [
  {
    icon: 'lucide:handshake',
    title: 'Relation',
    desc: "Peu de clients, beaucoup d’attention. Des relations longues, pas à la chaîne.",
    badge: true, // 👈 macaron certif
  },
  {
    icon: 'lucide:check-circle-2',
    title: 'Utilité',
    desc: 'Des solutions qui répondent à un besoin concret. Pas de gadgets.'
  },
  {
    icon: 'lucide:wand-2',
    title: 'Simplicité',
    desc: 'Process clairs, réponses rapides, friction minimale. La clarté au service de la confiance.'
  },
  {
    icon: 'lucide:scale',
    title: 'Justesse & transparence',
    desc: 'Prix justes, promesses tenues, origine et partenaires annoncés clairement.',
    badge: true,
  },
  {
    icon: 'lucide:factory',
    title: 'Sourcing local & maîtrisé',
    desc: 'Approvisionnements européens, fournisseurs connus, produits fiables & adaptés.'
  },
  {
    icon: 'lucide:heart-handshake',
    title: 'Impact',
    desc: 'À terme : opportunités pour des jeunes + réseau de partenaires éthiques.'
  },
];
---

<!-- Fond plein largeur en lilas créatif -->
<section id="about" aria-labelledby="about-title" class="w-full bg-creative scroll-m-4">
  <div class="mx-auto max-w-6xl px-4 py-16">
    <header class="max-w-3xl">
      <p class="inline-flex items-center gap-2 rounded-full bg-bg-alt px-3 py-1 text-xs font-medium text-gray-700">
        <Icon name="lucide:compass" class="h-4 w-4 text-primary" aria-hidden="true" />
        À propos
      </p>
      <h2 id="about-title" class="mt-3 text-2xl md:text-3xl font-bold text-gray-900">
        CPEP — Mission, vision & valeurs
      </h2>
      <p class="mt-4 text-gray-700">
        Nous concevons des <strong>outils de communication durables et responsables</strong> — print & web — pour des entreprises qui veulent
        <strong>s’inscrire dans le temps</strong>. Sérieux d’un bon brief, énergie d’un
        <span class="font-display"> CPEP<span class="inline-block text-accent rotate-[8deg] origin-bottom-left">!</span></span> :
        ça part en prod, propre.
      </p>
    </header>

    <!-- Mission / Vision -->
    <section class="mt-10 grid gap-6 md:grid-cols-[1fr,2fr] items-start">
      <div class="rounded-xl border border-bg-alt bg-white p-5">
        <h3 class="inline-flex items-center gap-2 text-lg font-semibold text-gray-900">
          <Icon name="lucide:target" class="h-5 w-5 text-primary" aria-hidden="true" /> Mission
        </h3>
        <p class="mt-3 text-gray-700">
          Accompagner nos clients dans leur <strong>communication digitale et physique</strong> avec des
          <strong>sites éco-conçus</strong> et des <strong>supports utiles & durables</strong>. Des partenariats
          de confiance, majoritairement en France et en Europe, avec une <strong>expérience fluide, réactive et humaine</strong>.
        </p>
      </div>

      <div class="rounded-xl border border-bg-alt bg-white p-5">
        <h3 class="inline-flex items-center gap-2 text-lg font-semibold text-gray-900">
          <Icon name="lucide:sparkles" class="h-5 w-5 text-primary" aria-hidden="true" /> Vision
        </h3>
        <p class="mt-3 text-gray-700">
          Faire de la communication un <strong>levier de transformation positive</strong> : <strong>responsable, utile, élégante, durable</strong>.
          Devenir la référence d’une <strong>com’ qui a du sens</strong> — sans jetable ni bullshit — où une bonne idée,
          bien exécutée, <strong>dure</strong>.
        </p>
      </div>
    </section>

    <!-- Valeurs (mappées) -->
    <section class="mt-10">
        <h3 class="text-lg font-semibold text-gray-900">Nos valeurs</h3>

        <ul class="mt-6 grid gap-6 sm:grid-cols-2 lg:grid-cols-3">
            {values.map((v) => (
            <li class="relative rounded-xl border border-bg-alt bg-white p-5 hover:border-accent/60 hover:shadow-sm transition">
                {/* Badge certif en haut-gauche (seulement si v.badge === true) */}
                {v.badge && (
                    <span class="absolute -top-2 -right-2 z-10 pointer-events-none select-none" aria-hidden="true">
                        <span class="inline-grid h-7 w-7 place-items-center rounded-full bg-info shadow-md">
                        <Icon name="lucide:circle-check-big" class="h-4 w-4 text-white" stroke-width="2.4" />
                        </span>
                    </span>
                )}

                <div class="flex items-start gap-3">
                <Icon name={v.icon} class="h-5 w-5 mt-0.5 text-primary" aria-hidden="true" />
                <div>
                    <p class="font-semibold text-gray-900">{v.title}</p>
                    <p class="mt-1 text-sm text-gray-700">{v.desc}</p>
                </div>
                </div>
            </li>
            ))}
        </ul>
    </section>


    <!-- Callout baseline -->
    <aside class="mt-10 rounded-xl border border-bg-alt bg-primary/5 p-5">
      <div class="flex flex-wrap items-center gap-3">
        <Icon name="lucide:badge-check" class="h-5 w-5 text-primary" aria-hidden="true" />
        <p class="m-0 text-sm text-gray-700">
          <strong>Moins de volume, plus de qualité</strong> — clients fidèles, fournisseurs européens, obsession du détail.
        </p>
      </div>
    </aside>

    <!-- JSON-LD AboutPage -->
    <script type="application/ld+json">
    {JSON.stringify({
      "@context": "https://schema.org",
      "@type": "AboutPage",
      "name": "À propos de CPEP",
      "description": "CPEP conçoit des outils de communication durables et responsables — web éco-conçu et supports print utiles — avec des partenariats européens et un service fluide, réactif et humain.",
      "about": {
        "@type": "Organization",
        "name": "CPEP",
        "slogan": "La plus juste expression de votre identité",
        "areaServed": "FR",
        "brand": "CPEP",
        "knowsAbout": [
          "Emballages personnalisés durables",
          "Sacs, boîtes, étuis, calages, étiquettes",
          "Éco-conception web, SEO technique",
          "Sourcing européen"
        ]
      }
    })}
    </script>
  </div>
</section>
